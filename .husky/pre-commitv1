# npm test
# Get changed files
CHANGED_FILES=$(git diff --name-only --cached | grep '^src/.*\.ts$' | grep -v '\.test\.ts$')

./node_modules/.bin/tsc

#for file in $CHANGED_FILES; do
#  npx jscodeshift -t ./dist/pre-commit-scripts/codeShift.js $file
#done

#./node_modules/.bin/tsc --outDir ./commit-script ./pre-commit-scripts/codeShift.ts && \
regexString=$(node ./dist/pre-commit-scripts/codeShift.js $CHANGED_FILES)

npx jest --testNamePattern="$regexString"
#npx jest --findRelatedTests $CHANGED_FILES
